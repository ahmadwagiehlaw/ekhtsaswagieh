# خطة التنفيذ النهائية — تطبيق "اختصاصي"

**الحالة:** مُعتمدة بعد مراجعة تقريري الأصلي + تقرير التحقق (Gemini) + مراجعة إضافية.
**لسه معملتش أي تعديل في الكود** — دي وثيقة الاتفاق النهائية قبل البدء.

---

## رأيي في تقرير التحقق (Gemini)

التقرير دقيق فعلاً ومفيد، وأكّد كل حاجة ذكرتها صح. بس فيه 3 نقط محتاجة تعديل قبل ما نلتزم بيها، لأن تنفيذها زي ما هي ممكن "يبوظ التطبيق" فعلاً زي ما انت قلقان منه، أو يفتح ثغرة جديدة مكان القديمة:

### تعديل 1: حل حقل "المدعي/الطاعن" المقترح فيه خطورة مبالغ فيها
Gemini اقترح **تغيير المفتاح الأساسي في قاعدة البيانات لـ `plaintiff`** (تسمية إنجليزية موحدة). ده حل "نظيف" نظرياً، بس **خطير عملياً**: معناه لازم نعمل migration لكل قضية موجودة فعلاً في Firestore لكل المكاتب (تينانتس)، وأي مكان في الكود بيقرا أو يكتب `'المدعي'`/`'الطاعن'` مباشرة (عشرات الأماكن) لازم يتغير مع بعض في نفس اللحظة، وإلا هتتلخبط البيانات بمنتصف الطريق. ده تحديداً نوع التغيير اللي بيسبب "نبوظ التطبيق".

**البديل الأكثر أماناً:** نسيب المفتاح المخزّن زي ما هو (`الطاعن`، لأنه ده اللي بالفعل بيتحفظ حالياً)، ونمنع `AddCaseModal.jsx` من تكرار عملية "إعادة التسمية" دي أصلاً لو مش لازمة، وأهم حاجة: **نضيف fallback في كل الأماكن اللي بتقرا `'المدعي'` بس** (زي `TasksManagerModal.jsx`) عشان تقرا `'الطاعن'` كمان — بالظبط زي ما `SmartDocumentsTab.jsx` بيعمل بالفعل. تعديل إضافي (additive) مش تعديل هدام، صفر مخاطرة على البيانات الموجودة.

### تعديل 2: حل استعلامات SuperAdmin بـ Cloud Function مبالغ فيه لحجم المشكلة
Gemini اقترح "Cloud Function لحساب الإحصائيات". ده معناه لازم نفعّل خطة Firebase المدفوعة (Blaze) ونبني ونعمل deploy لسيرفرلس function جديدة تماماً — بنية تحتية إضافية لصفحة إدارية واحدة بيستخدمها شخص واحد (أنت) مش كتير. مبالغة تقنية.

**البديل العملي:** تحويل الحلقة المتتالية (for-loop بينتظر كل استعلام لوحده) لتشتغل **بالتوازي** (`Promise.all`)، وتقليل عدد مرات إعادة الجلب الكامل (تحديث بيانات المستشار الواحد اللي اتغيّر بس، مش كل القايمة من الأول، بعد كل toggle). كده بنحل نفس المشكلة (بطء + قراءات زيادة) من غير ما نضيف بنية تحتية جديدة نديرها ونصرف عليها.

### تعديل 3: قاعدة Firestore الخاصة بـ `isSuperAdmin` — لازم متتنفذش لوحدها
Gemini اقترح تغيير `isSuperAdmin()` من إيميل ثابت لفحص `role == 'super_admin'`. الاقتراح صح من ناحية المبدأ، **بس التنفيذ الحرفي وحده خطير**، لسبب لقيته وأنا بتأكد من الملف بنفسي:

---

## 🔴 اكتشاف جديد لم يُذكر في أي من التقريرين السابقين — الأخطر في كل المراجعة

**الملف:** `firestore.rules:52` و`firestore.rules:55`

```
allow create: if isAuthenticated() && request.auth.uid == userId;
allow update: if isAuthenticated() && (request.auth.uid == userId || isSuperAdmin());
```

القاعدتين دول بيسمحوا لأي مستخدم مسجل دخول (أي حساب Firebase Auth، حتى موظف عادي أو مستشار جديد سجل بدعوة صالحة) إنه يكتب **أي قيمة يحبها** في مستند `users_directory` بتاعه هو، من غير أي قيد على القيم. ومنها الحقلين الأخطر في النظام كله: `role` و`tenantId`.

**يعني عملياً:** أي مستخدم عنده حساب في التطبيق (حتى تجريبي، أو موظف سابق فُصل من مكتب) يقدر يفتح Developer Console في المتصفح وهو داخل التطبيق، ويكتب سطر واحد بيستخدم Firebase SDK اللي أصلاً محمّل في الصفحة:

```js
setDoc(doc(db, 'users_directory', myUid), { role: 'consultant', tenantId: 'رقم-اشتراك-أي-مكتب-تاني' })
```

وبعدها فوراً بيبقى عنده **قراءة وكتابة كاملة على كل بيانات القسم التاني** (كل القضايا، المستندات، الإعدادات) — لأن `hasTenantAccess(tenantId)` هيرجّع `true` ليه. ورقم الاشتراك (`tenantId`) ده مش سري أصلاً — بيظهر بالنص الصريح في شاشة "المستخدمون" لأي موظف في القسم (شفته بنفسي في `SettingsUsersTab.jsx`)، فسهل ينتشر عن طريق سكرين شوت أو موظف قديم فاكره.

**هذا أخطر من مشكلة مفتاح R2 نفسها**، لأنه بيدي وصول كامل (قراءة **وكتابة**) على قاعدة البيانات الأساسية نفسها، مش بس ملفات مرفقة، ومش محتاج حتى معرفة كود برمجي — مجرد نسخ ولصق سطر واحد في الـ console.

**عشان كده، لو نفّذنا اقتراح Gemini (تغيير `isSuperAdmin` لفحص الـ role) *لوحده* من غير ما نصلح القاعدتين دول الأول، هنبقى فتحنا باب كمان: أي حد يكتب `role: 'super_admin'` لنفسه هيبقى عنده صلاحيات المدير العام الكاملة على النظام كله.** الأولوية الحقيقية مش تغيير `isSuperAdmin()` — الأولوية هي **قفل مين يقدر يكتب في `role`/`tenantId` من الأساس**، والاتنين لازم يتنفذوا **مع بعض في نفس الـ deploy**.

---

## الخطة النهائية المعتمدة (مقسّمة بمراحل، كل مرحلة معزولة عن اللي بعدها)

### 🔵 مرحلة صفر — إجراءات فورية خارج الكود (اليوم، قبل أي حاجة تانية)
هذول أهم من أي سطر كود، ومفيش داعي ننتظر عليهم:

1. **تدوير مفتاح Cloudflare R2** من لوحة تحكم Cloudflare مباشرة.
2. **تقييم حساسية `سجل القضايا.xlsx`** — لو بيانات حقيقية، لازم قرار بشأن تنظيف تاريخ الـ git (عملية دقيقة، هنجهزلها خطوة بخطوة منفصلة وقت تنفيذها، ومش هتتعمل عشوائي).
3. لو حد استخدم رقم اشتراك (tenantId) لمكتبه في أي مكان عام (سكرين شوت، دعم فني، إلخ) خلال الفترة اللي الثغرة كانت فيها موجودة — يستحسن اعتباره "معروض" ونتابع لو فيه أي وصول غريب لحد ما نقفل الثغرة في المرحلة الجاية.

### 🔴 مرحلة 1 — إغلاق ثغرات الأمان في الكود (أولوية قصوى، كل تعديل معزول ومُختبر لوحده)

**الترتيب هنا إلزامي — ممنوع نفّذ خطوة قبل اللي قبلها:**

1. **إصلاح `firestore.rules` (القاعدتين المكتشفتين حديثاً) — الأول قبل أي حاجة:**
   - منع أي مستخدم من كتابة `role` أو `tenantId` في مستند نفسه إلا في حالات محكومة (إنشاء أول مرة بقيمة افتراضية آمنة، أو من خلال مسار الدعوة `invite` المُتحقق منه فقط).
   - هذا التعديل قاعدة أمان فقط (server-side)، **مفيهوش أي تغيير في كود الواجهة**، وأثره صفر على أي مستخدم شغال حالياً بطريقة سليمة — فرصة كسر التطبيق منه معدومة تقريباً، لكن لازم نختبره بحساب تجريبي قبل النشر للتأكد إن تسجيل موظف جديد وإنشاء مستشار جديد بالدعوة لسه شغالين.
2. **بعد ما نتأكد إن الإصلاح فوق شغال ومُختبر:** نغيّر `isSuperAdmin()` من الإيميل الثابت لفحص `role == 'super_admin'` من `users_directory` — ودلوقتي آمن لأن حد محدش يقدر يكتب القيمة دي لنفسه.
3. **نقل رفع الملفات لمسار آمن (presigned URL)** — أكبر تغيير في المرحلة دي، محتاج قرار منك: نبني الـ backend الوسيط كـ **Cloudflare Worker** (الأنسب، لأن الـ bucket أصلاً على Cloudflare، ومجاني في حدود الاستخدام الحالي)، ولا Firebase Cloud Function؟ هستنى قرارك قبل ما أبدأ فيها، لأنها بنية تحتية جديدة مش مجرد تعديل كود موجود.

### 🟠 مرحلة 2 — إصلاح الأعطال الوظيفية الحرجة (كل إصلاح معزول، بيتنفذ ويتختبر لوحده)

بالترتيب من الأبسط/الأقل خطورة للأعقد:

1. **`Trash.jsx`** — إصلاح استدعاء `showConfirm` (تعديل سطرين، صفر أثر جانبي).
2. **`JudgmentRulesSection.jsx`** — تصحيح قيمة `'إجرائي'` غير الموجودة في قوائم التصنيف الافتراضية (تعديل سطر واحد).
3. **مشكلة "المدعي/الطاعن"** — بالطريقة الآمنة (إضافة fallback في أماكن القراءة الناقصة)، مش إعادة تسمية المفتاح.
4. **`checkDuplicateCase`** — تغييرها لتفحص `rawCases` بدل `cases` المفلترة (تعديل سطر، لكن **لازم نختبره كويس**: بعد التعديل، احتمال يظهرلك تنبيه "قضية مكررة" على أرقام كانت بتتسجل عادي قبل كده لو فيه قضية "لا شأن" بنفس الرقم فعلاً موجودة أصلاً — ده سلوك صحيح ومقصود، بس محتاج تعرفه قبل ما يحصل مفاجأة).
5. **معاينة/طباعة القوالب (`SmartDocumentsTab.jsx`)** — ربطها بالقضية الفعلية المفتوحة. محتاج تتبع دقيق لكل الأماكن اللي بتنادي على المعاينة دي (شاشة إدارة القوالب العامة لسه محتاجة "قضية تجريبية" للمعاينة، بينما الفتح من جوه قضية معينة لازم ياخد بيانات القضية دي بالظبط) — هنفرق بين السيناريوهين صراحة بدل ما نفترض واحد بس.
6. **توحيد منطق "آخر جلسة/قرار"** بين `AddSessionModal.jsx` و`CaseDetails.jsx` في دالة واحدة مشتركة — ده بيمس مسار حفظ الجلسات اللي بيتستخدم كل يوم، فهيحتاج اختبار يدوي شامل (إضافة جلسة جديدة، تعديل جلسة قديمة، التأكد إن قواعد الأحكام التلقائية لسه بتشتغل صح بعد التعديل) قبل ما نعتبره خلص.
7. **توحيد مودالات المهام التلاتة** (`TasksManagerModal`, `GlobalTasksModal`, `CaseTasksModal`) في hook واحد — أكبر تعديل في المرحلة دي، وهو اللي بيصلح باج "إلغاء الإنجاز" وباج تخزين `true` بدل نص. قبل ما نبدأ فيه، هكتب مواصفة واضحة لأي سلوك من التلاتة هو "الصح" (بناءً على السلوك السليم في الشاشتين التانيين)، عشان الشاشة التالتة تتصلح تتطابق معاهم مش العكس.

### 🟡 مرحلة 3 — توحيد الديون التقنية (أعلى خطورة تقنياً، تحتاج اختبار موسّع، تتعمل بعد استقرار المرحلتين اللي فوق)

1. استخراج محرك قواعد الأحكام التلقائية (المكرر في 3 أماكن) لملف واحد مشترك `judgmentRulesEngine.js`.
2. توحيد قوائم "أسماء الحقول البديلة" (رقم الدعوى/السنة/الصفة/إلخ) في `constants/caseFields.js` واستيرادها بدل التكرار، ملف بملف مش دفعة واحدة.
3. **قرار منتج محتاج رأيك:** هل نسيب الكلمات الافتراضية الثابتة في `roleHelpers.js` (طاعن/مدعي/إلخ) كشبكة أمان حتى لو المستشار خصص قايمة الصفات بنفسه، ولا نخليها قابلة للتعطيل الكامل لو المستشار عدّل القايمة يدوياً؟ الاتنين ليهم مميزات وعيوب، وده مش قرار تقني بحت.
4. مراجعة الاعتماد على نصوص عربية ثابتة في حساب الإحصائيات والمواعيد (`statsUtils.js`) — أكبر وأخطر تعديل في المشروع كله لأنه بيمس كل أرقام لوحة القيادة الحالية. هيتعمل آخر حاجة، وهقارن الأرقام قبل وبعد التعديل على بياناتك الحقيقية قبل ما نعتبره خلص، عشان مفيش إحصائية تتغير من تحتك من غير ما تاخد بالك.

### 🟢 مرحلة 4 — أداء ونظافة (منخفضة الخطورة، تتعمل في أي وقت متاح)

1. توازي استعلامات SuperAdmin (`Promise.all` بدل حلقة متتالية).
2. تحويل سجل النشاطات لجلب عند الطلب بدل مستمع دائم.
3. **`sw.js`**: كخطوة أولى سريعة وآمنة، تغيير اسم الـ cache (`CACHE_NAME`) فوراً عشان أي مستخدم حالي يجبر ياخد نسخة جديدة بدل ما يفضل عالق على القديمة، وده بيحل جزء من المشكلة فوراً بأقل مخاطرة. بعدين لاحقاً، وكخطوة منفصلة، نستبدله بحل رسمي (`vite-plugin-pwa`) مع اختبار دقيق لسلوك التحديث والعمل بدون إنترنت قبل ما ننشره، عشان ده بالذات نوع تعديل لو غلط ممكن "يبوّظ" تجربة المستخدمين اللي مثبتين التطبيق كـ PWA على موبايلاتهم.
4. تنظيف ملفات المستودع الزيادة (الأيقونات المكررة، `index_vanilla.html`، `sessions_block.txt`، `.firebase/`، `.wrangler/`) — كوميت منفصل تماماً عن أي تعديل وظيفي، صفر خطورة.

---

## قواعد السلامة اللي هلتزم بيها في التنفيذ (بناءً على طلبك)

- **كل مرحلة (وكل بند داخلها) هتتنفذ كـ commit منفصل**، عشان لو حصل أي مشكلة نقدر نرجع خطوة واحدة بس من غير ما نلغي كل الشغل.
- **قبل أي تعديل بيمس مسار بيانات موجود فعلاً** (الجلسات، فحص التكرار، المهام)، هوضحلك بالظبط السلوك القديم مقابل الجديد قبل ما أنفذ، مش بعد.
- **مفيش تغيير في شكل البيانات المخزنة (schema) بدون migration مدروسة منفصلة** — زي ما وضحت في تعديل 1 فوق، مش هنعمل rename لمفاتيح موجودة في بيانات حقيقية إلا لو قررنا سوا إننا محتاجينه فعلاً ومستعدين لخطوة الـ migration بشكل منفصل ومخطط له.
- بعد كل مرحلة هعمل `npm run build` (وتشغيل فعلي لو ممكن) للتأكد إن مفيش حاجة اتكسرت قبل ما ننتقل للمرحلة اللي بعدها.

---

## محتاج قرارك في نقطتين قبل ما أبدأ التنفيذ

1. **رفع الملفات (مرحلة 1، بند 3):** Cloudflare Worker ولا Firebase Cloud Function للـ presigned URLs؟
2. **من نبدأ؟** أقترح نبدأ بمرحلة 1 (الأمان) فوراً بما إنها الأخطر، ثم مرحلة 2. قولّي لو عايز ترتيب مختلف.
