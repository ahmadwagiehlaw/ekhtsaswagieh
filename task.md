- [x] 1. إضافة زر "مسح الكل" في واجهة إدخال الحكم.
- [x] 2. استحداث حالة (State) جديدة باسم `triggerValue` (مُحفز التعبئة) وتعديل الواجهة لتعرضها كأول حقل.
- [x] 3. ربط حقل "مختصر الحكم" بالحالة `type` وتعديل واجهته.
- [x] 4. برمجة الاستنتاج الآلي لـ "فئة الحكم" (`cat`) وإزالة خيار (Checkbox) "حكم نهائي".
- [x] 5. تحديث دالة `applyRules` لتقرأ من `triggerValue` كشرط وتملأ `type` كنتيجة.
- [x] 6. تعديل مسميات الحقول في مكون `JudgmentRulesSection.jsx` (الإعدادات) لتعكس (المحفز) و(مختصر الحكم) بدلاً من (نوع الحكم).
