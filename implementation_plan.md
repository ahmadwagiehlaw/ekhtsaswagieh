# خطة إصلاح وإعادة هيكلة صفحة الإعدادات الشاملة (مُحدَّثة)

لقد تمت مراجعة ما تم تنفيذه في الكود الفعلي، وفيما يلي الحالة الحالية للخطة مع بعض الاقتراحات الإضافية:

## 🔴 المرحلة 1: إصلاح الأعطال الحرجة
- ✅ **إصلاح تبويب "بيانات ونسخ" المكسور:** تم نقل سجل النشاطات وتفعيل المكون `<SettingsDataTab />`.
- ✅ **تفعيل تبويب "المستخدمون":** تمت الإضافة بنجاح وتفعيل المكون `<SettingsUsersTab />`.
- ✅ **إزالة كود تسجيل الدخول الميت:** تم الحذف بنجاح ووضع رسالة "غير مصرح لك بالدخول".
- ✅ **إصلاح حفظ التفضيلات وإضافة واجهتها:** 
  - ✅ تم نقل الحالة (`memoCalculationMode`, `scratchpadPosition`, `searchTabPosition`) إلى `SettingsSystemTab.jsx` وتم ربطها بدالة الحفظ.
  - ✅ تم بناء واجهة المستخدم (UI) (أزرار التبديل/Select) في `SettingsSystemTab.jsx` لتعديل هذه القيم.

## 🟠 المرحلة 2: تحسين بنية المعلومات (Architecture)
- ✅ **نقل منطقة الخطر:** تم نقل "إعادة ضبط المصنع / حذف البيانات" إلى `SettingsDataTab.jsx`.
- ✅ **دمج تبويب "المواعيد":** تم نقل قسم المواعيد بالكامل إلى مكون `SettingsDeadlinesSection.jsx` ودمجه بنجاح ليصبح قسماً منسدلاً داخل تبويب "الجلسات والأحكام" (`judgments`).
- ✅ **إعادة التسمية:** تم تغيير اسم التبويب إلى "⚙️ عام".

## 🟡 المرحلة 3: الميزات الناقصة
- ✅ **تغيير كلمة مرور المستشار:** تم إضافة الدالة والواجهة بنجاح في `SettingsSystemTab.jsx`.
- ✅ **تخصيص تنبيهات المواعيد:** تم إضافة حقل `alertDaysBefore` لقواعد المواعيد في واجهة المستخدم في المكون الجديد.

## 🟢 المرحلة 4: التنظيف النهائي (Clean-up)
- ✅ تم حذف `officeName` و `handleLogin`.
- ✅ تم تنظيف الواردات (imports).
- ✅ دالة `migrateJudgmentRule` لا تزال موجودة (وهذا طبيعي ومطلوب للتوافقية).

---

## 💡 اقتراحات إضافية للتنفيذ (توصيات)
✅ **تم تنفيذ جميع الاقتراحات المذكورة:**
1. استكمال واجهة التفضيلات وإضافة عناصر التحكم.
2. إصلاح تبويب المواعيد الضائع ودمجه في "الجلسات والأحكام".
3. تطبيق التنبيهات المخصصة وإضافة حقل `alertDaysBefore`.
4. تجزئة مكون `Settings.jsx` عن طريق فصل المواعيد في مكون `SettingsDeadlinesSection.jsx`.

## User Review Required
تم استكمال تنفيذ جميع أجزاء الخطة والاقتراحات بنسبة 100%. يمكنك الآن اختبار الإعدادات واكتشاف التغييرات!
