# تقرير مراجعة شاملة — تطبيق "اختصاصي"

**تاريخ المراجعة:** 23 أغسطس 2026
**نطاق المراجعة:** فحص كامل للكود (قراءة فقط، بدون أي تعديل) — الأمان، الأداء، استخدام كوته Firebase/Cloudflare، قواعد العمل والإحصائيات، التعبئة التلقائية، تجربة المستخدم، نظافة المستودع.
**عدد أسطر الكود المفحوصة:** ~26,000 سطر JS/JSX عبر `src/`

---

## 0. ملخص تنفيذي

التطبيق مبني بأساس معماري جيد فعلاً: تقسيم الكود بالمسارات (`React.lazy`)، عزل كامل للبيانات بين المكاتب (multi-tenant عبر `tenants/{tenantId}`)، عدم استخدام `window.confirm/alert` نهائياً (نظام حوارات موحد)، ضغط الصور قبل الرفع، وتخزين المرفقات في Object Storage بدل تضخيم Firestore. **لكن** المراجعة كشفت عن **مشكلة أمنية حرجة واحدة تستوجب تدخلاً فورياً خارج نطاق الكود**، بالإضافة لعدد من الأعطال الوظيفية الحقيقية التي بيواجهها المستخدم فعلياً بدون أي رسالة خطأ (بيانات بتتحذف من غير ما تتحذف، مذكرات بتتولّد ببيانات قضية غلط، مهام مش قادر تلغي إنجازها).

### أخطر 5 حاجات لازم تتعالج بالترتيب:

| # | المشكلة | الخطورة | الأثر |
|---|---|---|---|
| 1 | **مفتاح Cloudflare R2 السري مكتوب صراحة في كود الفرونت إند** ويترفع لكل زائر | 🔴 حرج جداً | أي حد يقدر ياخد المفتاح من المتصفح ويمسح/يعدّل كل ملفات كل المكاتب على R2 من غير ما يدخل التطبيق أصلاً |
| 2 | ملف Excel حقيقي (`سجل القضايا.xlsx`) بيانات قضايا فعلية متسجل في الـ git repo | 🔴 حرج | تسريب بيانات قضايا سرّية عبر تاريخ الـ git، حتى لو اتمسح دلوقتي |
| 3 | زرار "حذف نهائي" و"إفراغ السلة" في صفحة المحذوفات (Trash) **مبيعملوش حاجة إطلاقاً** | 🔴 حرج وظيفي | المستخدم بيفتكر إنه مسح البيانات وهي لسه موجودة (أو العكس بيحصل له لخبطة) |
| 4 | معاينة/طباعة قوالب المخاطبات بتاخد بيانات **أول قضية في القائمة** مش القضية اللي المستخدم فاتحها | 🔴 حرج وظيفي/قانوني | خطاب رسمي ممكن يطلع ببيانات قضية عميل تاني بالغلط |
| 5 | فحص "القضية مكررة" بيشتغل على قايمة مفلترة، فلو فيه قضية "لا شأن" بنفس الرقم مش هيتكشف التكرار (والفلتر ده شغال افتراضياً) | 🟠 عالي | إمكانية دخول قضايا مكررة بنفس الرقم بدون تنبيه |

باقي التقرير مفصّل بكل حاجة لقيناها، مقسّم حسب الموضوع.

---

## 1. الأمان (Security) — الأولوية القصوى

### 1.1 🔴 مفتاح R2 السري مكشوف بالكامل في كود العميل
**الملف:** `src/lib/r2.js:4-6`

```js
const ACCOUNT_ID = "27c3414283f515a8b9227c797020281f";
const ACCESS_KEY = "1aea7854d83695dd42dd0767654947e8";
const SECRET_KEY = "e0552a2d22c30ca79296e5d9772018a7d2634b209db99dda6f6befcfc3d3e648";
```

هذا الملف مستورد ومُستخدم في 9 ملفات مختلفة (`CaseDocuments.jsx`, `RollsLibrary.jsx`, `AddSessionModal.jsx`, `ProceduresModal.jsx`, `SessionTable.jsx`, `JudgmentsRollTab.jsx`, `UploadDocumentModal.jsx`, `CaseDetails.jsx`) — يعني هو المسار الأساسي لرفع كل مرفقات القضايا والرولات. بما إن الكود ده بيتبني (Vite build) ويتنشر كـ JS يشتغل في متصفح الزائر، فالمفتاح السري بيوصل **لأي حد بيفتح الموقع**، مش بس المستخدمين المسجلين — تقدر تستخرجه بسهولة بـ view-source أو أدوات المطور.

**الخطر الفعلي:** أي شخص عنده المفتاح ده يقدر يستخدم AWS SDK مباشرة (زي ما التطبيق بيعمل) ويقرأ/يمسح/يستبدل **أي ملف** في الـ bucket بتاع كل عملاء التطبيق (كل المكاتب/التينانتس)، من غير ما يمر على Firestore rules أصلاً لأنه مش بيتكلم مع Firebase، بيتكلم مباشرة مع Cloudflare R2.

**التوصية (إجراء عاجل خارج نطاق الكود قبل أي حاجة تانية):**
1. **إلغاء/تدوير (rotate) المفتاح السري ده فوراً من لوحة تحكم Cloudflare R2** — بغض النظر عن أي تعديل كود، لأن المفتاح الحالي لازم يتعتبر "محروق" بمجرد ما اتنشر مرة واحدة في أي بناء إنتاج سابق.
2. نقل عملية الرفع لتتم عبر Backend وسيط (Cloudflare Worker بسيط أو Firebase Cloud Function) بيولّد **presigned URL مؤقت** (صالح لدقايق) للعميل يرفع عليه مباشرة، بدل ما يحمل مفتاح دائم كامل الصلاحيات.
3. تفعيل CORS/scoped bucket policy تحصر المفتاح (لو فضل موجود مؤقتاً) على bucket واحد وعمليات محددة (PutObject فقط، مش قدرة على قراءة/مسح كل حاجة).

### 1.2 🔴 ملف بيانات حقيقي متسجل في الـ git repo
**الملف:** `سجل القضايا.xlsx` (84 كيلوبايت، في جذر المستودع)

الملف ده اتضاف بكومِت باسم "agneda22" (حسب `git log`)، وهو ملف Excel حقيقي — على الأرجح تصدير فعلي لبيانات قضايا من التطبيق أثناء التطوير/الاختبار. لو البيانات دي حقيقية (مش بيانات تجريبية)، فده تسريب فعلي لبيانات عملاء/قضايا سرّية داخل تاريخ الـ git، ومتاح لأي حد عنده access للريبو حتى لو اتمسح الملف دلوقتي (لسه موجود في التاريخ).

**التوصية:**
- التأكد فوراً هل البيانات دي حقيقية ولا تجريبية.
- لو حقيقية: إزالتها من **تاريخ الـ git بالكامل** (مش مجرد حذف الملف — لازم `git filter-repo` أو أداة مشابهة)، واعتبار أي بيانات فيها "متسربة" فعلياً (خصوصاً لو الريبو كان Public في أي وقت أو اتشير مع أي حد بره الفريق).
- إضافة قاعدة صريحة في `.gitignore` تمنع أي ملف `.xlsx`/`.csv` في الجذر مستقبلاً.

### 1.3 ملفات أدوات محلية متسجلة بالغلط في الـ git
**الملفات:** `.firebase/hosting.ZGlzdA.cache` (331 كيلوبايت)، `.firebase/hosting..cache`، `.wrangler/cache/wrangler-account.json`

دول ملفات cache محلية بتتولد تلقائي من أدوات النشر (`firebase deploy`, `wrangler`) ومفروض متتسجلش في الـ git إطلاقاً — `.wrangler/cache/wrangler-account.json` بالذات ممكن يحتوي على معلومات تعريف حساب Cloudflare. لازم تتضاف لـ `.gitignore`:
```
.firebase/
.wrangler/
```

### 1.4 `isSuperAdmin` في قواعد Firestore محصور بإيميل واحد Hardcoded
**الملف:** `firestore.rules:19`
```
function isSuperAdmin() {
  return isAuthenticated() && request.auth.token.email == 'ahmad.wagieh@gmail.com';
}
```
مش عيب أمني بحد ذاته (شغال صح)، لكن نقطة فشل واحدة (Single Point of Failure) — لو الإيميل ده احتاج يتغير أو يتضاف مدير تاني، لازم redeploy لقواعد الأمان بالكامل. الأفضل مستقبلاً استخدام `role == 'super_admin'` من `users_directory` بدل مقارنة إيميل ثابت (فيه فعلاً منطق `role` جاهز في نفس الملف مستخدم لـ `isConsultant()`).

### 1.5 Collections القديمة (Legacy) لسه مفتوحة في قواعد الأمان
**الملف:** `firestore.rules:70-88` — `appData`, `cases`, `rolls`, `tasks` (بدون tenant scoping) لسه مسموح فيها read/write لـ SuperAdmin "لأغراض الـ Migration فقط" حسب التعليق. لو عملية الترحيل (migration) للبيانات القديمة خلصت فعلاً، الأفضل حذف القواعد دي بالكامل لتقليل مساحة الهجوم (attack surface)، أو على الأقل التأكد إنها فاضية تماماً.

---

## 2. أعطال وظيفية حرجة (بتضرب المستخدم فعلياً)

### 2.1 🔴 صفحة المحذوفات (Trash): الحذف النهائي وإفراغ السلة ما بيحصلوش
**الملف:** `src/pages/Trash.jsx:26-53`

نظام الحوارات `showConfirm` في `UIContext.jsx` مبني على Promise لازم تعمله `await`:
```js
const confirmed = await showConfirm(title, message);
if (confirmed) { /* نفّذ */ }
```
كل مكان في التطبيق ماشي على النمط ده صح، **إلا** `Trash.jsx` اللي بيبعت دالة (callback) كباراميتر تالت بدل ما ينتظر الـ Promise:
```js
showConfirm('تأكيد الحذف النهائي', '...', async () => {
  await deleteCaseFromFirebase(id, true);
});
```
الباراميتر التالت في `showConfirm` مش callback — هو `confirmKey` (نص بيتحفظ في localStorage لتذكر "متسألنيش تاني"). يعني الدالة اللي المفروض تعمل الحذف **متتنفذش أبداً**. المستخدم بيشوف نافذة التأكيد، يضغط "تأكيد"، النافذة تقفل... وبس. القضية لسه موجودة في السلة.

**الأثر:** ميزة كاملة (حذف نهائي + إفراغ السلة) شكلها شغال بس مش بتعمل حاجة فعلياً — أخطر حاجة إنه مفيش أي رسالة خطأ توضح إن حاجة غلط.

### 2.2 🔴 قوالب المخاطبات: المعاينة بتاخد بيانات "أول قضية" مش القضية المفتوحة
**الملف:** `src/components/SmartDocumentsTab.jsx:188`

دالة `processPreview` (المسؤولة عن استبدال `{{رقم_الدعوى}}`, `{{المدعي}}`, إلخ ببيانات حقيقية) بتستخدم `cases?.[0]` — **أول قضية في مصفوفة القضايا كلها بتاعة القسم** — مش القضية اللي المستخدم فاتحها فعلياً. مفيش أي دالة تانية في الكود كله بتربط قالب بقضية محددة عن طريق الـ ID عند التوليد الفعلي.

**الأثر:** لو مستشار فتح شهادة/خطاب لقضية معينة وتوقع إنها هتتعبى ببيانات القضية دي، ممكن يطلعله بيانات قضية تانية خالص (أول قضية اتسجلت في النظام) — ده أخطر حاجة ممكن تحصل في تطبيق قانوني، لأنه ممكن يبعت خطاب رسمي فيه بيانات عميل غلط.

### 2.3 🟠 مهام سجل الإجراءات: تعارض بين 3 نسخ من نفس الكود (task modals)
**الملفات:** `TasksManagerModal.jsx`, `GlobalTasksModal.jsx`, `CaseTasksModal.jsx`

الـ 3 مكونات دول بينسخوا نفس منطق إدارة المهام (إضافة/تعديل/إنجاز/حذف) بدل ما يشتركوا في كود واحد، وده أدى فعلاً لتعارضات حقيقية:

- **`CaseTasksModal.jsx:135`** بيبعت `completeGlobalTask(task.id, newStatus === 'completed')` — يعني بيبعت **Boolean** (`true`/`false`) في مكان متوقع فيه نص (ملاحظات)، بينما `GlobalTasksModal.jsx:49` و`TasksManagerModal.jsx:189` بيبعتوا نص فعلي. النتيجة: حقل `notes` بتاع المهمة بيتخزن فيه `true` بدل نص، وأي مكان تاني بيحاول يقرا الملاحظة دي كنص (`ProceduresModal.jsx`, `SmartDocumentsTab.jsx`) هيطلعله `"true"` أو هيكسر.
- **إلغاء إنجاز مهمة مش شغال من داخل شاشة القضية**: `CaseTasksModal.jsx` مفيهوش الفرع اللي بيرجّع المهمة لـ "قيد الانتظار" لو كانت خلصت، عكس الشاشتين التانيين اللي بيعملوا الحالة دي صح. يعني لو فتحت مهمة من جوه القضية وضغطت "خلصت" بالغلط، مش هتقدر ترجعها من نفس المكان.
- تعديل مهمة نوعها "إطلاع" (`viewing`) من جوه `CaseTasksModal` بيحولها بالغلط لنوع "عام" (`general`) لأن نموذج التعديل مش بيحمّل الحقل `type` الأصلي.

**التوصية:** توحيد منطق CRUD للمهام في hook أو utility واحدة تستخدمها الثلاث شاشات، بدل النسخ المكرر.

### 2.4 🟠 فحص "القضية مكررة" بيتجاوز القضايا المفلترة (bypass فعلي بالإعداد الافتراضي)
**الملف:** `src/context/AppState.jsx:333` — `checkDuplicateCase` بيفحص التكرار على `cases` (القائمة **بعد** الفلترة بخيار "إخفاء لا شأن/خارج الاختصاص")، مش `rawCases` (القائمة الكاملة). وبما إن فلتر الإخفاء ده مفعّل افتراضياً (`globalHideNoInterest = 1`)، فأي قضية اتسجلت قبل كده بصفة "لا شأن" أو "خارج الاختصاص" **مش هتتفحص ضدها** عند تسجيل قضية جديدة بنفس الرقم — يعني ممكن تدخل نفس رقم القضية مرتين من غير أي تنبيه.

### 2.5 🟠 عدم تطابق مفاتيح الحقول: "المدعي" بيتسجل باسم "الطاعن"
**الملف:** `src/components/AddCaseModal.jsx:52-54`
```js
if (dataToSave['المدعي']) {
  dataToSave['الطاعن'] = dataToSave['المدعي'];
  delete dataToSave['المدعي'];
}
```
كل قضية بتتسجل من الفورم ده بيتخزن اسم المدعي فعلياً تحت المفتاح `الطاعن`، مش `المدعي`. لكن أماكن تانية بتقرا `caseData['المدعي']` مباشرة من غير أي fallback على `الطاعن`:
- `TasksManagerModal.jsx:953,978` — بحث عن قضية وعرض اسمها في قائمة ربط المهام بيطلع **فاضي** لأي قضية اتسجلت بالطريقة دي.

بينما `SmartDocumentsTab.jsx` بيتعامل صح (`getFieldVal(c, ['المدعي', 'الطاعن', 'المدعى'])`). التناقض ده معناه نفس الحقل بيظهر صح في مكان وفاضي في مكان تاني لنفس القضية بالظبط.

### 2.6 🟠 تعديل جلسة قديمة بيغيّر "آخر جلسة/قرار" المعروض بدون تنبيه
**الملفين:** `AddSessionModal.jsx:90-102` و`CaseDetails.jsx:335-353` (نفس المنطق منسوخ في المكانين)

بعد إضافة أو تعديل أي جلسة، الكود بيعيد ترتيب الجلسات بالتاريخ وبيكتب فوق حقول "آخر جلسة/القرار/الرول/نوع الجلسة" بقيم **أحدث جلسة بالتاريخ**، مش بالضرورة الجلسة اللي المستخدم لسه دخّلها. لو مستشار رجع يسجل جلسة قديمة فاتته (جلسة الشهر اللي فات مثلاً)، أو عدّل ملاحظة في جلسة قديمة، ممكن "الحالة الحالية" المعروضة في كل الشاشات (وفي قوالب المخاطبات كمان) تتغير من غير ما يقصد وبدون أي تأكيد أو تنبيه.

### 2.7 قواعد الأحكام الافتراضية: قيمة تصنيف غير موجودة أصلاً في القوائم
**الملف:** `src/components/JudgmentRulesSection.jsx:84` — زرار "توليد القواعد الافتراضية" بيولّد قاعدة لحالة "وقف جزائي" بتحط `actions.classification = 'إجرائي'`، لكن القيمة دي **مش موجودة إطلاقاً** في قايمة `judgmentClassifications` الافتراضية (`['صالح', 'ضد', 'مختلط', 'اعتبار', 'وقف جزائي', 'وقف تعليقي', 'خبراء']` في `AppState.jsx:27`). النتيجة: القاعدة دي مبتظهرش أصلاً في القائمة المنسدلة، وأي حكم يتصنف بيها هيقع في سلة "غير مصنف" في الإحصائيات بدل ما يتحسب صح.

---

## 3. الديون التقنية والتكرار (Tech Debt & Duplication)

هذا القسم هو الأخطر من ناحية "قابلية الصيانة" — نفس المنطق مكتوب في أكتر من مكان، وأي تعديل مستقبلي معرّض يتصلح في مكان وينسى في التاني.

### 3.1 محرك "قواعد الأحكام التلقائية" مكرر 3 مرات بالكامل
نفس خوارزمية مطابقة الشروط (`roleMatch`/`categoryMatch`/`classificationMatch`/`typeMatch`/`sessionTypeMatch`/`decisionMatch`) مكتوبة بشكل منفصل في:
- `src/components/SessionTable.jsx:289-309`
- `src/components/CaseDetailsTabs/SessionsTab.jsx:311-334`
- `src/components/BulkJudgmentRegistrationModal.jsx:36-55`

وفيه فروقات فعلية بين النسخ التلاتة (مش نفس الكود بالظبط):
- `SessionsTab.jsx` بيقرا الصفة مباشرة من الحقول (`caseData['الصفة'] || caseData['صفة']`) بدل استخدام الـ helper المشترك.
- `BulkJudgmentRegistrationModal.jsx` بيبدأ بـ `_role: ''` فاضي وممكن قواعد الصفة متتطبقش خالص في التسجيل الجماعي.

**التوصية:** استخراج المنطق ده لدالة واحدة مشتركة (`applyJudgmentDefaultRules(caseData, session, judgmentDefaults)`) تتنادى من الثلاث أماكن.

### 3.2 حساب "الصفة" (طاعن/مطعون ضده) بمنطق يمنع التخصيص الحقيقي
**الملف:** `src/constants/roleHelpers.js:6-20`

الدوال `isAppellantRole`/`isAppelleeRole` بتفحص أول قيمة في `settings.roles` المُعرّفة من المستخدم، **لكن بعدها بترجع تفحص كمان 3 كلمات ثابتة في الكود** (`'طاعن'`, `'مستأنف'`, `'مدعي'` وهكذا) كـ fallback دايم الوجود. معنى كده: لو مستشار خصص قايمة الصفات في الإعدادات وشال كلمة "مدعي" (لأنه عايز يستخدمها لمعنى تاني في ممارسته)، النظام هيفضل يصنفها "طاعن" برضه — التخصيص من الإعدادات مش بيلغي السلوك الافتراضي فعلياً، بس بيضيف عليه.

### 3.3 قواعد المواعيد وتصنيف الكسب/الخسارة معتمدة على مطابقة نصوص عربية Hardcoded وهشّة
**أمثلة (كلها في `src/utils/statsUtils.js`):**
- `rule.name.includes('الطعن')` و`rule.name.includes('وقف')` (سطر 236، 264) — قاعدة الميعاد بتتحدد حسب **اسم القاعدة النصي** اللي المستخدم كاتبه في الإعدادات، مش حقل نوع منفصل. لو المستشار غيّر اسم القاعدة (حتى لو مجرد صياغة تانية)، القاعدة بتوقف عن الشغل **من غير أي تحذير**.
- 3 نسخ منفصلة من منطق "استنتاج كسب/خسارة الحكم" (`computeMonthStats`, `calculateDashboardStats`, وحساب `latestJudgmentSession`) كلهم بيفحصوا نفس الكلمات (`'رفض'`, `'قبول'`, `'فحص'`) بشكل منفصل — أي تعديل لازم يتعمل 3 مرات.
- تصنيف "وقف جزائي"/"اعتبار" بيعتمد على `.includes('وقف جزائي')` نصياً في أكتر من مكان (`statsUtils.js`, `caseUtils.js`) — لو اتغير اسم التصنيف من الإعدادات، الإحصائيات كلها بتفقد الدقة بصمت.

**المشكلة الأعمق:** الإحصائيات والمواعيد مبنية على *نصوص* اختيارية المستخدم يقدر يغيّرها من شاشة الإعدادات، لكن منطق الحساب معتمد على القيم الافتراضية نفسها كـ hardcoded strings. ده تناقض تصميمي — إما تمنع تعديل القيم دي من الإعدادات، أو تربط الحساب بمعرّف ثابت (id) بدل النص المعروض.

### 3.4 تصنيف "كسب/خسارة" غلط لأطراف الخصومة الثالثة
**الملف:** `src/utils/statsUtils.js:209-213` (ومكرر في `computeMonthStats` سطر 85-88)

لو الصفة مش "طاعن" ولا "مطعون ضده" (زي "خصم مدخل"/"خصم متدخل" الموجودة أصلاً في قايمة الصفات الافتراضية)، الكود بيحطها تلقائياً في نفس اتجاه "الطاعن" — يعني قضية طرف ثالث فيها رفض بتتحسب "خسارة" حتى لو الطرف الثالث ده مالوش علاقة أصلاً بنتيجة الحكم. ده بيوزّن إحصائيات الكسب/الخسارة غلط.

### 3.5 تكرار قوائم "أسماء بديلة للحقل" في أكتر من مكان
حقل زي "رقم الدعوى" بيتقرا بمفاتيح مختلفة (`'رقم الدعوى'`, `'رقم القضية'`, `'رقم_الدعوى'`) في **4 أماكن منفصلة** بقوائم مش متطابقة بالظبط:
- `caseUtils.js` (الـ helper "الرسمي" المفروض يكون المرجع الوحيد)
- `AppState.jsx` (نسختين: `checkDuplicateCase` و`createNewCase` — **وبالفرق ده نفسه سبب المشكلة #2.4/2.5**، لأن `createNewCase` ناقصة مفتاح `'رقم_الدعوى'` و`'year'` اللي موجودين في `checkDuplicateCase`)
- `statsUtils.js` (نسخة تالتة inline)
- `SmartDocumentsTab.jsx` (نسخة رابعة inline بدل استيراد الثابت المشترك من `constants/caseFields.js`)

نفس الموضوع مع `COURT_FIRST_NO_KEYS` في `constants/caseFields.js` — **متعرّف ومش مستخدم في أي مكان تاني**، بينما `SmartDocumentsTab.jsx` بيكرر نفس القيمة يدوياً بدل ما يستورد الثابت.

### 3.6 التباس بين إعداد "درجة التقاضي" و"التخصص"
**الملف:** `src/context/AppState.jsx:250-251, 355-356` (منسوخة مرتين)
```js
const isSupreme = currentCourtDegree === 'ثان درجة' || currentCourtDegree === 'عليا' || currentCourtDegree === 'الإدارية العليا';
```
`'الإدارية العليا'` هي القيمة الافتراضية لحقل **"التخصص"** (`courtSpecialization`)، مش حقل **"درجة التقاضي"** (`courtDegree`) اللي بيتفحص هنا — يبدو إنه خلط بين الحقلين وقت الكتابة. الشرط ده عملياً مش هيتفعّل غالباً بالقيمة دي إلا لو حصل تشابه صدفة في القيم.

### 3.7 حقول شكلها متاحة للتخصيص لكنها مش فعلياً
**الملف:** `src/components/JudgmentRulesSection.jsx` — شرط `conditions.sessionType` موجود في هيكل بيانات القاعدة ومُستخدم فعلياً وقت التقييم في 3 أماكن (انظر 3.1)، **لكن مفيش أي عنصر واجهة (dropdown) لتحديده** في شاشة تحرير القاعدة — يعني موجود بالبيانات القديمة بس مش قابل للإنشاء أو التعديل من الشاشة الحالية.

---

## 4. الأداء واستخدام كوته Firebase / Cloudflare

الخلاصة العامة: المعمارية سليمة نسبياً (تقسيم صحيح للتينانتس، كل الـ `onSnapshot` listeners بتتنضف صح عند الخروج من الصفحة، الصفحات التانية مش بتعيد جلب بيانات موجودة أصلاً في الـ context)، لكن فيه نقطتين تستاهلوا انتباه:

### 4.1 🟠 حلقة N+1 في لوحة SuperAdmin
**الملف:** `src/pages/SuperAdmin.jsx:36-46` — بيجيب كل المستشارين، وبعدين **لكل مستشار على حدة** بيعمل استعلام `getCountFromServer` منفصل جوه loop، وده بيتكرر تاني من الصفر بعد أي عملية توجل بسيطة (تفعيل لوجو، حظر، توليد دعوة). لو عدد المكاتب/التينانتس زاد، الصفحة دي هتبقى أبطأ خطياً وهتستهلك قراءات (reads) أكتر من اللازم في كل مرة تتفتح أو يتغيّر فيها أي إعداد بسيط.

### 4.2 🟡 مستمع مباشر (Live) على سجل النشاطات بدل تحديث يدوي
**الملف:** `src/pages/AuditLogs.jsx:26-38` — سجل النشاطات مفتوح كـ `onSnapshot` مباشر (حتى 1000 مستند)، فأي حركة لأي موظف في القسم (حفظ/حذف/تعديل) بتعيد تنزيل السجل كامل لأي أدمن فاتح الصفحة دي، رغم إن سجل النشاطات عادة بيتفحص بشكل دوري مش لحظي. تحويلها لجلب لمرة واحدة (`getDocs`) + زرار "تحديث" هيقلل القراءات بشكل ملحوظ.

كذلك دالة "تنظيف السجلات القديمة" (`AuditLogs.jsx:78`) بتقرا **كل** سجل النشاطات (مش بس اللي أقدم من 3 شهور) عشان تفلتر محلياً — الأفضل استخدام استعلام Firestore بشرط `where('timestamp', '<', ...)` مباشرة.

### 4.3 لا يوجد حد أقصى (limit/pagination) على استعلام القضايا
**الملف:** `src/lib/firebase.js` (`getCasesRef`) — مفيش أي `limit()` على الاشتراك المباشر بكل قضايا القسم. مع مكتب كبير عنده آلاف القضايا (متضمنة القديمة/المؤرشفة)، كل تاب مفتوح بيحمّل ويحتفظ بكل القضايا في الذاكرة طول الوقت — مقبول لعدد قضايا متوسط، لكن يستاهل مراقبة مع نمو البيانات.

### 4.4 نقاط إيجابية تستاهل تسجيل
- المرفقات والصور بتتخزن في Cloudflare R2 (رابط بس في Firestore)، مش base64 جوه المستند — قرار صحيح يوفر تكلفة وحدود حجم Firestore.
- ضغط الصور (`browser-image-compression`) شغال فعلياً قبل الرفع في `CaseDocuments.jsx` و`RollsLibrary.jsx`.
- كل الـ dependencies المثبتة في `package.json` (`docxtemplater`, `pizzip`, `xlsx`, `@aws-sdk/client-s3`, `browser-image-compression`) مُستخدمة فعلياً — مفيش وزن زيادة في الحزمة النهائية من مكتبات متثبتة ومش مستخدمة.
- تقسيم الكود بالمسارات (`React.lazy` في `App.jsx`) وتقسيم الـ vendor chunks (`vite.config.js` — firebase/vendor/files_engine/ui منفصلين) — ده تحسين أداء حقيقي وموجود فعلاً.

---

## 5. Service Worker (PWA) — مشكلة تحديث محتملة

**الملف:** `public/sw.js`

الـ Service Worker شغال بنمط "Cache First" بسيط لكل طلب (`caches.match(event.request) → لو مش لاقي، fetch`)، وبيعمل cache لروابط CDN خارجية **مش مستخدمة أصلاً في البناء الحالي** (`cdn.tailwindcss.com`, Font Awesome عبر cdnjs, `xlsx.full.min.js` عبر cdnjs) — دي على الأرجح باقية من نسخة `index_vanilla.html` القديمة (نسخة HTML واحدة بتحمل كل حاجة من CDN)، مش من التطبيق الحالي المبني بـ Vite واللي بيستخدم `xlsx`/Tailwind كـ npm packages محلية.

**المخاطر:**
- تحميل روابط CDN مش مستخدمة = هدر في مساحة الـ cache وزمن التثبيت (install) الأول للـ Service Worker.
- إستراتيجية Cache-First بلا أي منطق تحديث/`skipWaiting`/`clients.claim` واضح تعني المستخدم ممكن **يفضل عالق على نسخة قديمة من التطبيق** حتى بعد ما ترفع تحديث جديد، لحد ما الـ cache يتاكل/يتغير الاسم يدوياً.

**التوصية:** إعادة النظر في `sw.js` بالكامل — إما استخدام أداة PWA رسمية متكاملة مع Vite (`vite-plugin-pwa`) بتدير الـ cache versioning تلقائي، أو على الأقل تنظيف قايمة الروابط لتطابق الأصول الفعلية المستخدمة، وإضافة منطق تفعيل فوري للنسخة الجديدة.

---

## 6. نظافة المستودع (Repo Hygiene)

| الملف | الحجم | الملاحظة |
|---|---|---|
| `سجل القضايا.xlsx` | 84 كيلوبايت | **بيانات حقيقية محتملة** — انظر البند 1.2 |
| `index_vanilla.html` | 106 كيلوبايت | نسخة قديمة كاملة من التطبيق (HTML واحد بيحمل من CDN)، مش الـ entry point الحالي (`index.html` الفعلي 1.3 كيلوبايت بس) — وزن ميت في المستودع |
| `sessions_block.txt` | 32 كيلوبايت | قطعة كود JSX خام (يبدو نسخة احتياطية يدوية وقت تعديل تبويب الجلسات) — مالهاش لازمة في المستودع |
| `icon-512.png` / `icon-192.png` (في الجذر) | 245+35 كيلوبايت | **نسخة مكررة بالضبط** من نفس الملفات الموجودة في `public/icon-512.png`/`public/icon-192.png` — مفيش داعي لوجودهم في الجذر |
| `.firebase/`, `.wrangler/` | 331+ كيلوبايت | ملفات cache لأدوات نشر محلية، مفروض تتضاف لـ `.gitignore` — انظر البند 1.3 |
| `extract.cjs`, `refactor.cjs`, `update_casedetails.cjs`, `create_icons.py` | صغيرة | سكريبتات تطوير لمرة واحدة (one-off) في جذر المشروع — لو خلصت مهمتها، الأفضل نقلها لمجلد `scripts/` منفصل أو حذفها بدل ما تفضل في الجذر جنب كود التطبيق |

**ملاحظة عامة:** الملفات دي مش بتأثر على أداء التطبيق المنشور فعلياً (لأن Firebase Hosting بينشر مجلد `dist` بس، الناتج من البناء)، لكنها بتضخّم حجم الـ git repo وسرعة الـ clone، وبعضها (خصوصاً ملف الإكسيل) بيمثل مخاطرة أمنية حقيقية.

---

## 7. تجربة المستخدم (تقييم كأنك مستخدم فعلي)

### نقاط قوة حقيقية
- **مفيش أي `window.confirm`/`window.alert` في الكود كله** — كل تأكيد وتنبيه بيمر من نظام حوارات موحد ومصمم (`UIContext`)، ده حرفياً نادر في تطبيقات بالحجم ده ويدي إحساس احترافي متسق.
- ميزة "تراجع" (Undo) بعد حذف قضية موجودة فعلاً — لمسة نادراً ما تتلاقي في تطبيقات مشابهة.
- التصميم "mobile-first" فعلاً ملموس في الكود (breakpoints منتشرة صح، أحجام أزرار مناسبة للمس في أغلب الأماكن).
- تقسيم الشاشات بالتحميل الكسول (lazy load) — الشاشة الأولى بتفتح أسرع.

### مشاكل حقيقية في تجربة الاستخدام

**7.1 تداخل/تكرار بصري بين 3 مفاهيم اسمها فيه "رول"**
- **الأجندة** (`Agenda.jsx`) — فيها تبويب "رول الجلسات" و"رول الأحكام".
- **مكتبة الرولات** (`RollsLibrary.jsx`) — أرشيف لصور/PDF رولات ممسوحة ضوئياً من المحكمة (حاجة مختلفة تماماً — ملفات مرفوعة، مش بيانات مُنشأة من التطبيق).
- **DayRoll** (`DayRoll.jsx`) — صفحة مستقلة لطباعة رول يوم واحد، **مش موصولة بأي زرار أو رابط من أي مكان في التطبيق** (كود يتيم على الأرجح)، وبتعمل نفس وظيفة نافذة الطباعة الموجودة أصلاً جوه الأجندة (يعني فيه مسارين مختلفين لنفس وظيفة "طباعة رول اليوم" ممكن يختلفوا في الشكل بمرور الوقت).

سكرتيرة أو موظف جديد هيتلخبط بسهولة أوي بين "مكتبة الرولات" و"رول الجلسات" — الاسمين قريبين جداً بس المحتوى مختلف كلياً.

**7.2 أزرار عائمة (FAB) بتتصادم بصرياً**
في وضع "المودال" لعرض القضية (فتحها من نافذة بحث/معاينة سريعة)، زرار "إضافة قضية" العام من `Layout.jsx` بيفضل ظاهر في نفس الإحداثيات اللي فيها أزرار تانية من `CaseDetails.jsx` (زرار المستندات، زرار إنشاء الوثائق) — على شاشة موبايل صغيرة ده ممكن يبان تكديس أزرار فوق بعض أو صعوبة الضغط على الزرار الصح.

**7.3 Tooltips ملموسة باللمس مش موجودة**
تفاصيل شريط الأداء في لوحة القيادة (`Dashboard.jsx`) بتظهر بالـ hover بس — على الموبايل (اللمس) المعلومة دي عملياً مش قابلة للوصول خالص لأنه مفيش hover على شاشة لمس.

**7.4 مفيش حالة "بداية فارغة" ترحيبية في لوحة القيادة**
مستشار جديد لسه ما دخلش أي قضية هيشوف لوحة قيادة كاملة بكل الرسوم البيانية والأرقام صفر، بدل رسالة ترحيبية واضحة توجهه لإضافة أول قضية.

**7.5 تفاصيل صغيرة**
- تعليق تطوير داخلي متسرّب في الكود المنتج (`Dashboard.jsx:68`) — مش بيأثر على المستخدم لكنه علامة على استعجال في آخر تعديل.
- `console.log` باقي في كودين (تركيب التطبيق كـ PWA، وأخطاء دمج ملفات Word) — الأفضل تحويلهم لرسائل توست مرئية للمستخدم بدل console مخفي.

---

## 8. تقييم شامل — التطبيق كمنتج

| المحور | التقييم | الملاحظة |
|---|---|---|
| **البنية المعمارية العامة** | 🟢 جيد جداً | multi-tenant سليم، تقسيم كود بالمسارات، فصل بين البيانات والعرض |
| **الأمان** | 🔴 ضعيف حالياً | مفتاح R2 مكشوف = مشكلة حرجة تلغي أي تقييم إيجابي تاني لحد ما تتحل |
| **سلامة البيانات (Data Integrity)** | 🟠 متوسط | تكرار مفاتيح الحقول ومنطق مكرر يخلق فرص لبيانات متضاربة بصمت |
| **الأداء وكوته السيرفر** | 🟢 جيد | لا يوجد تسريب listeners، لا يوجد إعادة جلب زيادة، نقطة ضعف واحدة واضحة (SuperAdmin N+1) |
| **تجربة الاستخدام (UX)** | 🟡 جيد مع ثغرات وظيفية | تصميم متسق واحترافي، لكن أعطال حقيقية (زي زرار الحذف اللي مش شغال) بتكسر الثقة |
| **جاهزية للتوسع (Scale)** | 🟡 يحتاج مراجعة | استعلام القضايا بدون حد أقصى، عدم وجود pagination — هيحتاج انتباه مع نمو عدد القضايا/المكاتب |
| **نظافة الكود والصيانة** | 🟠 متوسط | منطق مكرر 3 مرات في أكتر من مكان (قواعد الأحكام، مودالات المهام) — أي تعديل مستقبلي معرض لعدم الاتساق |

**الانطباع العام كمستخدم:** التطبيق بيدي إحساس احترافي ومصمم بعناية من ناحية الواجهة والتفاعل — ده مش تطبيق "مسودة"، فيه تفكير حقيقي في تجربة المستخدم (الحوارات الموحدة، الـ Undo، الضغط والتصميم المتجاوب). لكن تحت السطح فيه ديون تقنية حقيقية بسبب النمو السريع للمزايا (نسخ-لصق منطق بدل إعادة استخدامه) أدت لأعطال فعلية غير مرئية للمطور لحد ما تحصل للمستخدم في سيناريو معين (زي حذف جلسة قديمة، أو فتح مهمة من شاشة معينة بدل التانية).

---

## 9. خطة الأولويات المقترحة

### فورية (خارج نطاق الكود، اليوم)
1. تدوير مفتاح Cloudflare R2 السري.
2. تقييم حساسية بيانات `سجل القضايا.xlsx` وإزالتها من تاريخ الـ git لو حقيقية.

### عاجلة (قبل أي تطوير جديد)
3. إصلاح `Trash.jsx` (الحذف النهائي/إفراغ السلة).
4. ربط معاينة/طباعة القوالب بالقضية الفعلية المفتوحة بدل `cases[0]`.
5. إصلاح فحص التكرار ليشتغل على `rawCases` بدل القائمة المفلترة.
6. توحيد مفتاح "الطاعن/المدعي" في كل أماكن القراءة والكتابة.
7. نقل الرفع لـ presigned URLs بدل مفتاح دائم في الفرونت إند.

### مهمة (تحسين جودة/صيانة)
8. توحيد محرك قواعد الأحكام في دالة واحدة مشتركة بدل 3 نسخ.
9. توحيد مودالات المهام الثلاثة (أو استخراج hook مشترك).
10. مراجعة اعتماد الإحصائيات/المواعيد على نصوص hardcoded، وربطها بمعرّفات ثابتة بدل الاسم المعروض القابل للتغيير.
11. تنظيف `sw.js` أو استبداله بحل PWA رسمي.

### تحسينات تراكمية (وقت متاح)
12. تنظيف ملفات المستودع الزيادة (`index_vanilla.html`, `sessions_block.txt`, أيقونات مكررة، `.firebase/`, `.wrangler/`).
13. إضافة `limit()`/pagination على استعلام القضايا استعداداً للنمو.
14. تحويل سجل النشاطات لجلب يدوي بدل اشتراك مباشر دائم.
15. إعادة تسمية/توضيح الفرق بين "مكتبة الرولات" و"رول الجلسات"، ومراجعة مصير `DayRoll.jsx` (حذف أو ربط).

---

*هذا التقرير بحثي بالكامل (قراءة كود فقط) ولم يتم تعديل أي ملف في المشروع أثناء إعداده.*
